from setuptools import find_packages, setup

setup(
    name='SegNet',
    packages=find_packages(),
    version='0.1.0',
    description=' image segmentation is to train a neural network to output a pixel-wise mask of the image. ',
    author='caihao.cui',
    license='MIT',
)
